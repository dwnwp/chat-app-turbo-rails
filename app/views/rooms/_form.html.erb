<div id='actions'>
<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='create-room'>
    <%= form.label :name, style: "display: inline; color: white", required: true %>
    <%= form.text_field :name, class: 'message-form-input' %>
    <%= form.submit "Create a room", class: 'button' %>
  </div>

<% end %>
</div>